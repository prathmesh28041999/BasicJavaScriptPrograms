function rectangleArea(width, height) {
    let area = width * height;
    
    //return area;
    //if we  When a function is called, the computer will run through the function’s 
    //code and evaluate the result of calling the function. By default that resulting value is undefined.
    
  }
  console.log(rectangleArea(5, 7)) // Prints undefined